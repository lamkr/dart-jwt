import 'package:dart_jwt/ext/date_time_extension.dart';
import 'package:dart_jwt/interfaces.dart';
import 'package:test/test.dart';

void main() {
  test('With instant claim should use default impl', () {
  });

  test('With instant claim should use default impl and handle null', () {
  });
}
